You are a culinary expert and an advanced recipe extractor. 
I am going to provide you with the scraped text of a recipe web page.

Your tasks:
1. Extract the recipe details: title, cuisine, prep_time, cook_time, total_time, servings, difficulty. Limit difficulty to one of: "easy", "medium", "hard". Defaults to "medium" if unclear.
2. Extract the ingredients exactly as they appear but structured into quantity, unit, and item.
3. Extract the step-by-step instructions as a list of strings.
4. Generate an accurate nutritional estimate (calories as int, protein as string, carbs as string, fat as string per serving).
5. Generate exactly 3 ingredient substitutions (e.g. 'Replace butter with olive oil').
6. Generate a shopping list categorized correctly (e.g., "dairy": ["butter"], "pantry": ["flour"]).
7. Suggest exactly 3 related recipes that pair well with this dish.

URL: {url}

Scraped Content:
{content}
